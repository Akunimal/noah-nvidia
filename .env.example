# Frontend
VITE_API_BASE_URL=http://localhost:8000
# Local-only bearer for a synthetic tenant. Never set this on a public build:
# VITE_* values are bundled into the browser.
VITE_NOAH_AUTH_TOKEN=demo-owner

# Backend values are documented in services/api/.env.example.
